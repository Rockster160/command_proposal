<% if @task.iterations.many? %>
  <table class="cmd-table">
    <thead>
      <th>Status</th>
      <th>Timestamp / Link</th>
      <!-- <th># Comments</th> -->
      <!-- <th>Diff</th> -->
    </thead>
    <tbody>
      <% @task.iterations.where.not(id: @iteration&.id).order(created_at: :desc).each do |iteration| %>
        <tr>
          <td><%= iteration.status.capitalize %></td>
          <td><%= link_to iteration.created_at.strftime("%b %-d, %Y at %H:%M"), cmd_path(@task, iteration: iteration) %></td>
          <!-- <td><%= iteration.comments.count %></td> -->
          <!-- <td><%= link_to "Diff", cmd_path(@task, iteration: @iteration, diff: iteration) %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
